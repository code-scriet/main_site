export default {
  plugins: {
    // Tailwind v4: the PostCSS plugin moved to its own package, and autoprefixing
    // is built in (no separate `autoprefixer` plugin needed).
    '@tailwindcss/postcss': {},
  },
}
