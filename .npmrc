# This monorepo's dependency tree requires relaxed peer resolution: a plain
# `npm install` otherwise fails with ERESOLVE, and the committed lockfile is
# generated with --legacy-peer-deps. Setting this makes `npm install` work for
# contributors and keeps lockfile regens reproducible. CI installs via `npm ci`.
legacy-peer-deps=true
